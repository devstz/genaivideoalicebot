# Скопируйте в .env и заполните. См. config/settings.py.

TOKEN=
# Опциональный fallback для ссылок t.me (QR, 2FA, UTM), если getMe недоступен.
# При валидном TOKEN username подтягивается из Telegram API один раз при старте.
BOT_USERNAME=

DATABASE_URL=
